# Python >= 3.12 (river >= 0.26 needs >= 3.11; tensorflow >= 2.21 needs >= 3.10).
# The old stack was held at Python 3.9 by tensorflow==2.8.0, which pins
# numpy<1.24 and protobuf==3.20.3; that cascade is what blocked capymoa
# (numpy~=2.2, pandas~=2.2, scikit-learn~=1.7), not the Python version itself.

numpy==2.2.6
scipy==1.17.1
scikit-learn==1.7.2
pandas==2.3.3
PyYAML==6.0.2
river==0.26.1
tensorflow==2.21.0
tqdm==4.67.1
wandb==0.23.0
matplotlib==3.10.9
seaborn==0.13.2
absl-py==2.3.1
folktables==0.0.12
torch==2.9.1
capymoa==0.14.0
